# Necromantle — characters, class screens, and abilities plan

Written 2026-09-05 after Milestone 9. This replaces the earlier sketch. Companion: `music-plan.md`.

## What exists

- One class, the Arcane Sorcerer, as the only entry in `content/classes.ts` (`ClassDef`: model, weapon mesh, resource, six ability ids, animation table, accent colour).
- `Player` is built from a `ClassDef`; `abilities/system.ts` is a registry keyed by ability id; the HUD orb reads the class resource name and colour class.
- The KayKit Adventurers pack on disk already has the Knight, Rogue (hooded), and Barbarian rigs with the same clip names as the Mage, so every class below uses animations we have.
- No title screen or character select. The game boots into the Sorcerer on the first canvas click. One save (`necromantle.save`).

## Roster (original names)

| Class | Rig | Weapon kept | Resource | Rhythm | Accent |
|---|---|---|---|---|---|
| Arcane Sorcerer (done) | Mage | 2H staff | Arcane Energy: generated by Bolt, spent on Orb | ranged, crowd control, teleport | violet `#8B5CF6` |
| Sepulcher Knight | Knight | 1H sword + shield | Fury: +8 per hit dealt, +12 per hit taken, decays 6/s out of combat | stands in the pack, throws it back | iron blue `#6FA8DC` |
| Grave Hunter | Rogue (hooded) | crossbow | Focus: fills 10/s while standing still, +15 per kill, spent on volleys | ranged physical, traps, backflips | bone green `#7ED957` |
| Pale Reaver | Barbarian | 2H axe | Blood: +10 per kill, +3 per hit, drains 4/s; Frenzy unlocks at 100 | fast melee, momentum, life steal | blood red `#C0392B` |

### Abilities (six per class; slots 1–6 match the skill bar)

**Sepulcher Knight**

| Slot | Ability | Kind | Cost / CD | Effect | Feel |
|---|---|---|---|---|---|
| 1 | Cleave | generator | free | 120° arc, 2.6 m, weapon damage ×1.0, three-hit chain with the third hit knocking back | sword trail, hit-stop on the third |
| 2 | Judgement | spender | 60 Fury | leap-slam at the soft target within 8 m, 4 m ring, ×3.5, stagger | screen shake, stone chips decal |
| 3 | Shield Rush | cooldown 8 s | — | 9 m dash behind the shield, knocks down everything in the lane | shield glow, dust trail |
| 4 | Iron Ward | cooldown 14 s | — | absorb shield for 30 % of max HP for 6 s, taunts within 6 m | rune ring rises around the Knight |
| 5 | Grave Stomp | cooldown 10 s | — | 5 m ring stagger, 1.5 s slow | crack decal, low thud |
| 6 | Bulwark | cooldown 25 s | — | pulls enemies within 10 m to 3 m and roots them for 2 s | chains VFX (reuse the necromancer pull ribbon) |

**Grave Hunter**

| Slot | Ability | Kind | Cost / CD | Effect |
|---|---|---|---|---|
| 1 | Bolt Shot | generator | free | fast crossbow bolt, pierces one, +6 Focus on hit |
| 2 | Fan of Bolts | spender | 40 Focus | seven bolts in a 60° cone, ×0.6 each |
| 3 | Vault | cooldown 6 s | — | backflip 6 m away from the aim point, 0.5 s invulnerable |
| 4 | Caltrops | cooldown 12 s | — | 4 m field, 4 s, slows 50 % and bleeds |
| 5 | Mark | cooldown 15 s | — | soft target takes +30 % damage for 8 s, drops a globe on death |
| 6 | Rain of Bolts | cooldown 30 s | — | 6 m ground-target area, 3 s of falling bolts |

**Pale Reaver**

| Slot | Ability | Kind | Cost / CD | Effect |
|---|---|---|---|---|
| 1 | Rend | generator | free | wide axe swing, applies Bleed (5 s), +Blood on hit |
| 2 | Whirl | spender | 8 Blood per 0.25 s, channelled | spin, moves at 70 % speed, hits everything within 3 m |
| 3 | Leap | cooldown 9 s | — | jump to the ground target within 10 m, 3.5 m ring on landing |
| 4 | Frenzy | cooldown 20 s | — | +40 % attack speed and move speed for 6 s; free at full Blood |
| 5 | Bleed Storm | cooldown 14 s | — | every bleeding enemy within 8 m takes its remaining bleed at once |
| 6 | Harvest | cooldown 30 s | — | 8 s: every kill heals 8 % and refunds Blood |

Each class keeps the loop generate → position → spend → clear. Ability defs stay in `content/abilities.ts` with a `classId` so the debug panel and tooltips can filter them.

## Screens

### 1. Title screen (`src/ui/title.ts`, `title.css`)
- Full-screen HTML over a slow camera drift through the Hollowmere hub with the sky and fog already running (the scene loads behind it, so there is no second loading stage).
- Wordmark "NECROMANTLE" in the storyboard type, gold on carved stone. Buttons: Continue (last played class), New Journey, Settings (music and sfx sliders that already exist in the dev panel), Credits (asset licences).
- Audio unlock happens on the first click here instead of on the canvas.

### 2. Character select (`src/ui/classSelect.ts`)
- Four pedestals in a row inside a dedicated small Babylon scene (or a walled corner of the hub level) with a rotating idle of each rig, lit by one warm key and the class accent as rim colour (the rim plugin already exists).
- Right panel: class name, one-line fantasy, resource description, six ability cards (icon, name, kind, cost, one sentence). Hover a card to preview its VFX on the pedestal.
- Left panel: save slot for that class (level, area, play time) with Continue and Delete.
- Locked classes show the pedestal with a greyed rig and "In development" until built. Keyboard and mouse both work; Enter confirms.
- Save keys become `necromantle.save.<classId>`; the existing save migrates to the Sorcerer slot on first load (`SaveV2` adds `classId` and `playTime`).

### 3. In-game character sheet (extend `src/ui/inventory.ts`)
- Add a Skills tab beside the paper doll: six ability cards with full text, current numbers after gear and passives, unlock level, and the improvement each ability gets at levels 7 and 9.
- The passive picker moves into this tab so all character choices live in one place.

## Save system (local storage)

A versioned local save already exists (`persistence/save.ts`: `SaveV1` with level, xp, area index, inventory, equipment, passives; key `necromantle.save`; autosave every 10 s, on level entry, and on pickup). This phase grows it into a proper slot system without leaving `localStorage`.

- **Slots per class.** Key `necromantle.slot.<classId>` holding `SaveV2`: everything in V1 plus `classId`, `playTime` (seconds), `areaName`, `waveIndex`, `health` and `resource` at the last checkpoint, `settings` (music, sfx, SSAO, camera distance), `stats` (kills, elite kills, deaths, legendaries found), and `createdAt`.
- **Checkpoints.** Explicit save on: level entry, wave cleared, door unlocked, boss phase, NPC heal, inventory close, and the 10 s autosave. The title screen's Continue restores the checkpoint; dying restores it too, so death costs the current wave, not the level.
- **Migration.** On first load, a V1 record moves into the Sorcerer slot and the old key is removed. Unknown versions start fresh and keep the raw JSON under `necromantle.backup.<timestamp>` so nothing is silently lost.
- **Index record.** `necromantle.index` lists the slots with `lastPlayed`, so the title screen's Continue knows which class to open without parsing every slot.
- **Safety.** Every write goes through one `Save.commit(slot, data)` that JSON-serialises, checks the size against a 2 MB budget, and catches quota errors with a HUD toast ("Could not save: storage full"). Reads validate shape before use; a corrupt slot is renamed to a backup, not deleted.
- **Delete** from the character select asks for confirmation and removes the slot and its index entry.
- **Dev panel**: export the current slot as a JSON download and import from a file, for bug reports and testing. Still local storage underneath.

## Engineering steps

| # | Step | Files | Estimate | Acceptance |
|---|---|---|---|---|
| 1 | Title screen and audio unlock | `ui/title.ts`, `game.ts` boot path | 0.5 day | Game opens on the title over the live hub; Continue restores the save |
| 2 | Save slots (V2, index, migration, checkpoints, quota handling, export/import) | `persistence/save.ts`, `game.ts`, `ui/debug.ts` | 1 day | Old save lands in the Sorcerer slot; dying restores the last checkpoint; a full-storage write shows a toast instead of failing silently |
| 2b | Class select with 3D pedestals reading the slots | `ui/classSelect.ts`, `assets/registry.ts` (char.knight, char.hunter, char.reaver) | 1 day | Sorcerer playable from the select; other three appear locked; each slot shows level, area, play time |
| 3 | Melee primitives | `combat/melee.ts` (arc and lane hit queries from facing, lunge magnetism, weapon trail mesh), `player/animator.ts` (attack chains) | 1 day | A test ability swings an arc that hits the ring of ghouls in the harness |
| 4 | Resource generalisation | `player/player.ts` (`resource` with gain/decay rules from `ClassDef`), `ui/hud.ts` (orb colour, name, Frenzy full-state glow) | 0.5 day | Sorcerer unchanged; Fury decays out of combat in the harness |
| 5 | Sepulcher Knight | `content/classes.ts`, `content/abilities.ts`, `abilities/impl/knight.ts`, sfx names, VFX | 2 days | All six abilities against 8–24 packs; camera and hit reactions hold at melee range |
| 6 | Grave Hunter | same shape; crossbow projectile, cone, ground areas | 2 days | Kiting loop works: stand still to fill Focus, spend, vault |
| 7 | Pale Reaver | same shape; channelled Whirl, Bleed status in `enemies/enemy.ts` | 2 days | Whirl channel drains Blood; Frenzy unlocks at full |
| 8 | Skills tab | `ui/inventory.ts` | 0.5 day | Numbers match the tooltip math |
| 9 | Class-aware loot | `content/items.ts` (class restriction, per-class legendaries), `loot/generator.ts` | 1 day | A Knight never rolls a staff; each class has two legendaries |
| 10 | Storyboard, docs, gap analysis, captures | `docs/` | 0.5 day | One capture per class in the select and mid-combat |

Order: 1 → 2 → 2b → 3 → 4 → 5, then 8 and 9, then 6 and 7. The Knight goes first because melee stresses the camera, hitboxes, and enemy reactions the most.

## Risks

- Melee against the over-the-shoulder camera hides enemies behind the player. Mitigation: combat distance pulls back to 9.5 m for melee classes and the soft target gets a subtle ground marker.
- Four rigs share the animation clip names, but the attack clips differ per weapon (1H_Melee_Attack_Chop, 2H_Melee_Attack_Spin, and so on); the animation table per class is the place to reconcile them, and every clip is checked with the loader before the class is playable.
- The pedestal scene adds a second render loop while the hub is loading; keep it to four instanced rigs and one light so the title stays at 60 fps.

## Status (2026-09-05)

Every step above is built and committed.

| Step | Result |
|---|---|
| 1 Title screen | `ui/title.ts` over the live hub with a drifting camera; Continue, Choose a character, Settings, Credits. Audio unlocks on the first click there. |
| 2 Save slots | `persistence/save.ts` V2: one slot per class, index record, legacy migration into the Sorcerer slot, size budget, quota toast, backups for unreadable records, export/import, delete with confirmation. Death returns to the level checkpoint and resets the waves. |
| 2b Class select | `world/selectStage.ts`: four pedestals 90 m south of the square, rim-lit rigs, focused rig turns to the camera, ability cards preview their clip. Slot card shows level, area, play time. |
| 3 Melee primitives | `EnemyManager.queryArc` and `queryLane`, lunge magnetism, `Player.dash` for fixed-distance leaps, slash arcs in `vfx.ts`, melee sounds. |
| 4 Resources | `ClassDef.resource` rules (decay, per-kill, per-hit-taken, still-regen); the HUD orb recolours per class. |
| 5 Sepulcher Knight | `abilities/knight.ts`: Cleave chain, Judgement, Shield Rush, Iron Ward (absorb + taunt), Grave Stomp, Bulwark (pull + root). |
| 6 Grave Hunter | `abilities/hunter.ts`: Bolt Shot, Fan of Bolts, Vault, Caltrops (slow + bleed field), Mark (+30 %, globe on death), Rain of Bolts. |
| 7 Pale Reaver | `abilities/reaver.ts`: Rend (bleed), Whirl (channelled), Leap, Frenzy (free at full Blood), Bleed Storm, Harvest. |
| 8 Skills tab | Inventory gained an INVENTORY / SKILLS tab strip; the Skills view lists the six abilities with live damage after gear, costs, unlock levels and the level milestones. |
| 9 Class-aware loot | Weapons and legendaries carry `classId`; per-class weapon names; a physical-damage affix for the three new classes; two legendaries per class (Iron Tide, Gravebind Gauntlets, Twin String, Thorn Crown, Red Harvest, Iron Lung). Off-class items show in the bag but cannot be equipped. |
| 10 Captures | `docs/screenshots/select-<class>.png`, `m10-knight-*.png`, `m10-hunter-*.png`, `m10-reaver-*.png`, `m10-title.png`. |

Harness: `?play=<classId>` skips the title and select; `?new=1` wipes every slot.

Open tuning notes: the Knight's Fury fills fast against big packs (12 per hit taken); Rain of Bolts reuses the arcane ghost beam and reads violet; Mark spends its cooldown when no target is locked.
